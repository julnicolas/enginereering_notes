# List venv dependencies recursively
Shows all installed dependencies within a venv.

## Install
``` sh
pip install pipdeptree
```

## List installed dependencies and subdependencies
First, activate the venv, then:
``` sh
pipdeptree
```

