""" slice describes a slice object.
Slice objects are used, with __getitems__.

Check the related note for related information.

Then in terms of datastructures, a slice is a
bounded reference to an array or dynamic array.
"""

# Here is how to create a slice object
# first element is start, stop then step
slice(0, 3, 1)

# [1:2:3], this is what is passed to __getitem__
# as a key value

