# Install from packages.json
Note: `nodejs` and `npm` packages must be installed first.

To install all dependency groups:
``` sh
npm i
```

To restrict to the production group:
``` sh
npm i --production
```
